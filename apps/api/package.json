{
  "name": "@mercari-bot/api",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests for @mercari-bot/api yet'",
    "lint": "echo 'no lint configured for @mercari-bot/api'"
  },
  "dependencies": {
    "@mercari-bot/core": "workspace:*",
    "@mercari-bot/db": "workspace:*",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1"
  }
}
